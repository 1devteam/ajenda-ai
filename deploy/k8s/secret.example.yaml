apiVersion: v1
kind: Secret
metadata:
  name: ajenda-secrets
  namespace: ajenda
type: Opaque
stringData:
  AJENDA_DATABASE_URL: "postgresql+psycopg://ajenda:<REPLACE_POSTGRES_PASSWORD>@postgres:5432/ajenda"
  POSTGRES_PASSWORD: "<REPLACE_POSTGRES_PASSWORD>"
